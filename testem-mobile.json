{
  "framework": "mocha",
  "parallel": 1,

  "on_exit": "ps ax | grep ngrok | cut -f2 -d' ' - | xargs kill -9",

  "src_files": [
    "node_modules/chai/chai.js",
    "dist/test.bundle.js"
  ],

  "launchers": {
    "node": {
      "command": "./run.js",
      "protocol": "tap"
    }
  },

  "launch_in_ci": [
    "node"
  ]
}
