{
  "framework": "mocha",
  "parallel": 1,

  "src_files": [
    "node_modules/chai/chai.js",
    "dist/test.bundle.js"
  ],

  "launchers": {
    "node": {
      "command": "./run.js -d 2 -f version:=5.0.1",
      "protocol": "tap"
    }
  },

  "launch_in_ci": [
    "node"
  ]
}
