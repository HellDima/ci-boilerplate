machine:
    node:
        version: 4.2.2
    ports:
          - "8888:8888"

dependencies:
    pre:
        - npm install testem -g
        - npm install external-ip -g
        - npm install request -g
    override:
        - npm prune
        - npm install
        - npm update

test:
    override:
        - npm run test-bs

# deployment:
#     staging:
#         branch: /.*/
#         commands:
#             - npm run deploy
